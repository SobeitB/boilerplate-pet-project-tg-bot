{
  "name": "subtitle-bot",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:bot": "pnpm --filter bot dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^17.2.2",
    "eslint": "^9.9.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
